# 画像模板（`.scratch/e2e/profile.md`）

每项都要填。查不到的写「未知」并列进「待确认」，不猜。

```markdown
Verified: 2026-09-12

# <项目名> e2e 画像

## 执行
- exec 前缀：`docker compose exec e2e`
- 起环境：`docker compose --profile e2e up -d e2e`
- spec 目录：`tests/e2e/`
- 依赖前置：<跑测试前必须起的容器 / 必须存在的 feature flag / seed 脚本>

## 入口（容器视角）
| 端 | URL | 登录方式 | 备注 |
|---|---|---|---|
| dashboard | http://<容器>.orb.local/dashboard/ | 邮箱密码 | Vite allowedHosts 已放行 .local：是 / 否 |

## 账号（每个角色一个；密码只写来源，不写值）
| 角色 | 邮箱 | 密码来源 | 能登（核过日期） | 说明 |
|---|---|---|---|---|
| shop_admin | shop-a@example.test | .env E2E_SHOP_PASSWORD | 2026-09-12 | 店铺 id=1 |

强制 MFA 的角色：<怎么处理，或「只跑匿名冒烟」>

## 数据
- 快照：`task db:dump -- <库>`  /  恢复：`task db:restore -- <文件>`
- 没有可用手段 → 本行写 `只读`，全项目不生成写操作测试
- 共享库注意：<库在共享容器 / 线上共享库，破坏性 SQL 的额外限制>

## 副作用分级（有写操作的接口 / 页面动作）
| 级别 | 含义 | 本项目属于此级的操作 |
|---|---|---|
| 1 | 只读 | 所有 GET、列表、报表查看 |
| 2 | 库内可逆 | 建 / 改 / 删测试数据（恢复快照即回滚） |
| 3 | 库内不可逆 | 日次締め、账期出金、重置密码、组织转移 |
| 4 | 出库外部 | 支付网关、退款、邮件、LINE 推送、打印、R2 删除、机器翻译 |

3 / 4 级默认跳过；来源写清楚（文件 / 路由）。

## 业务口径（断言要用的规则）
- <例：税额必须经 effectiveTaxCategory(product, isTakeout) 算，前端不直接读 tax_category>
- <例：多租户——A 店账号访问 B 店资源必须 403>

## 已有测试
- 单元 / 集成：<数量、位置、CI 里跑不跑>
- 现有 e2e：<位置；「无」>

## 范围（本轮）
- 层：smoke / roles / risk
- 端：<全部 或 列出>
- 风险路径：<按 钱 / 不可逆 / 无测试 / 近期事故 排序的前 N 条，每条一句为什么>

## 待确认
- <只列真的查不到的>
```

画像来源优先级：项目 CLAUDE.md > compose / Taskfile > 路由注册代码 > 已有文档。文档和代码冲突以代码为准，冲突本身写进「待确认」。
